apiVersion: v1
kind: Service
metadata:
  name: manager-service
  labels:
    name: manager-service
spec:
  type: NodePort
  ports:
    - port: 8888
      nodePort: 30364
      protocol: TCP
  selector:
    app: manager-app